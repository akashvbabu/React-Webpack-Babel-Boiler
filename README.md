###Boilerplate Application for React, Webpack, Babel with ES6
#### Running Instructions 
```npm install
	npm run start```

*Application has been configured to use ECMA6 (babel-loader and es2015 presets to facilitate transpilation of EMCA6 to ECMA5)